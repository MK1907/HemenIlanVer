# Kopyalayın: copy .env.example .env  —  OPENAI_API_KEY satırına gerçek anahtarı yazın (sadece .env kullanın; commit etmeyin)
# Docker: bu değişken compose ile api konteynerine gider; ortam değişkeni appsettings.Local.json içindeki OpenAI anahtarından önceliklidir.
OPENAI_API_KEY=
